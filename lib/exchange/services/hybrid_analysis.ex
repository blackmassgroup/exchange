defmodule Exchange.Services.HybridAnalysis do
  # @public_url "www.hybrid-analysis.com/api/v2"
  # @api_key System.get_env("HYBRID_ANALYSIS_KEY")

  def submit(_url) do
    # {:ok, conn, ref} = Mint.HTTP.request(conn, "GET", "/foo", [], "")
    # {:ok, conn, ref} = Mint.HTTP.request(conn, "GET", "/bar", [], "")
    :ok
  end
end
